<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div>
      <% var num = allposts.length; %>
      <% for(var i = 0; i < num; i++) { %>
      <div>
          <% var id = allposts[i]._id; %>
          <%= allposts[i].post %> </br>
          <a href="/:id", value = <%=allposts[i]._id%> >edit post</a> <!--how to enable this only on this post-->
      </div>
    <% } %>
    </div>
  </body>
</html>